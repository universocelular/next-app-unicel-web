
import { DiscountModeClient } from "@/components/admin/discount-mode-client";

// This page now relies entirely on the AdminDataProvider for its data.
export default function DiscountModePage() {
  return <DiscountModeClient />;
}
