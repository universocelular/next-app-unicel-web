
import { FreeModeClient } from "@/components/admin/free-mode-client";

// This page now relies entirely on the AdminDataProvider for its data.
export default function FreeModePage() {
  return <FreeModeClient />;
}
